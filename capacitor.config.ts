import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'project-android',
  webDir: 'www',
   server:{
    url:"https://dev-app2.docsvault.com/"
  }

};

export default config;
